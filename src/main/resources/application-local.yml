spring:
  datasource:
    url: jdbc:postgresql://localhost:${POSTGRES_PORT:5432}/delivery_dispatch
    username: delivery_dispatch
    password: delivery_dispatch
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

app:
  jwt:
    secret: local-development-jwt-secret-change-me
  demo-data:
    enabled: ${DEMO_DATA_ENABLED:true}
